addons:
    apt:
        packages:
            - cloc
language: python
python:
    - 2.7
    - 3.4
    - 3.5
install:
    - pip install .
    - make devenv
script:
    - make test
    - make style
    - make doc
after_success:
    - make loc
    - bash <(curl -s https://codecov.io/bash)
